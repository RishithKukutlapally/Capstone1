# --- Secrets (NFR-01) --------------------------------------------------------
.env
*.key
*.pem

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# --- Generated vector index --------------------------------------------------
# storage/ is normally a build artifact rebuilt by `catalog-rag ingest`.
# It is committed here so the project can be cloned and run without an
# ingest step. Delete storage/ and re-run ingest if it ever fails to load.

# --- Runtime logs ------------------------------------------------------------
# Logs may contain user queries; per docs/pii-policy.md they stay local.
logs/

# --- Model caches ------------------------------------------------------------
.cache/
models/

# --- Streamlit ---------------------------------------------------------------
.streamlit/secrets.toml

# --- OS / editor -------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/

# NOTE: reports/ is deliberately NOT ignored. Per the Evidence-in-Repo Rule the
# RAGAS report, model comparison and sample answers MUST be committed.

# Claude Code local settings
.claude/
