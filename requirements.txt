# Product Catalog & Compliance Assistant
#
# Google Gemini is the only model provider. Embeddings and reranking run
# locally. pip + Python only, no Docker and no database server.
# Developed and tested on Python 3.12.

# --- Orchestration -----------------------------------------------------------
langchain==1.3.18
langchain-core==1.6.1
langchain-community==0.4.1
langchain-text-splitters==1.1.2

# --- LLM provider ------------------------------------------------------------
langchain-google-genai==4.3.7
google-generativeai==0.8.6

# --- Vector store (in-process, no server) ------------------------------------
# NOT present in every environment. If `pip list` does not show chromadb,
# install these two explicitly.
chromadb==1.5.9
langchain-chroma==1.1.0

# --- Lexical retrieval -------------------------------------------------------
rank-bm25==0.2.2

# --- Local embeddings and cross-encoder reranker -----------------------------
sentence-transformers==6.0.0
torch==2.6.0
transformers==5.16.1

# --- Structured output, config, resilience -----------------------------------
pydantic==2.13.4
python-dotenv==1.2.3
PyYAML==6.0.3
tenacity==8.5.0

# --- Evaluation --------------------------------------------------------------
ragas==0.4.3
datasets==5.0.1
pandas==3.0.5

# --- Interface ---------------------------------------------------------------
# streamlit is optional: the CLI covers ingestion, evaluation and asking.
streamlit==1.62.0

# --- Tests -------------------------------------------------------------------
pytest==9.1.1
